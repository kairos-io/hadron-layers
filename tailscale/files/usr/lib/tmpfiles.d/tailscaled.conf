# Seed /etc/default/tailscaled from the factory copy under /usr on first boot.
# The unit works without it; this only gives the usual override file a home,
# which matters because a system extension cannot write to /etc itself.
d /etc/default 0755 root root -
C /etc/default/tailscaled 0644 root root - -
